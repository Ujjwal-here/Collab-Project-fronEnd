import styles from "./Card.module.css";

const Tags = () => {
    return (
        <div className={styles.tags}>
            <p>React</p>
            <p>Nodejs</p>
        </div>
    );
};

export default Tags;
